define(function (require) {
  'use strict';

  // Dependencies.
  var Backbone = require('backbone');

  /**
   * Base collection.
   */
  var Collection = Backbone.Collection;

  return Collection;
});
